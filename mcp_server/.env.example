# Base URL of the Budget Planner FastAPI backend
BACKEND_API_BASE_URL=http://localhost:8000

# Timeout (seconds) for requests to the backend
BACKEND_REQUEST_TIMEOUT=10.0

# Host/port the MCP server listens on (streamable HTTP at http://HOST:PORT/mcp)
MCP_HOST=0.0.0.0
MCP_PORT=8001
